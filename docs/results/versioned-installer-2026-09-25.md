# バージョン別インストーラーの検証

ソースコード・テスト・ドキュメントはすべてOpenAI Codexで生成した。

2026年9月25日に、インストーラーの取得対象を実行時の最新安定版から**インストーラーごとに固定した版**へ変更しました。v0.1.4用とv0.9.9用を区別し、版番号・本体のサイズ・SHA256・本体を含むcommitのURLをインストーラーに組み込みます。実行時にLatestや配布manifestを検索する処理はありません。

この変更はインストーラーだけが対象です。DNS本体の通信処理や制限値を変更するものではありません。[実行手順](../installer.md)も参照してください。

## ホスト側の試験

| 確認項目 | 結果 |
|---|---|
| インストール・更新処理 | 614 assertions成功。既存ファイル・実行状態・設定の維持と復元を含む。 |
| HTTPS取得 | 149 assertions成功。許可したHTTPS URL、転送の制限、取得失敗などを確認。 |
| SHA256 | 25 assertions成功。標準ベクトルを含む照合を確認。 |
| Python試験 | 全33件成功。版別配布・生成・起動コマンドに関する新規16件を含む。 |
| 取得する版の固定 | v0.1.4用はv0.1.4、v0.9.9用はv0.9.9の本体とSHA256で成功。Latestへの問い合わせは0回、本体取得は1回。 |
| 別の版の混入 | 同じサイズでも別の版の本体やSHA256では拒否。現行ファイルを書き換えず、稼働タスクを停止しない。 |
| サイズ・SHA256不一致 | 余分なバイト、欠落したバイト、異なるSHA256を拒否。現行ファイルと稼働タスクを維持。 |
| 固定メタデータの検証 | 不正な版番号・サイズ・SHA256、版番号が異なるURL、branch名や不完全なcommit IDを含むURLを取得前に拒否。 |
| 既存動作の維持 | 同じファイルでもタスクを再起動。起動失敗時の復元、同時実行、設定変更、scheduleの競合、保存結果が不明な場合を確認。 |

インストーラーのLua試験の合計は788 assertionsです。Makefileに記載された試験コマンドを個別に実行し、DNS本体の回帰試験を含めて成功しています。これらは模擬HTTP・ファイルシステム・ルーターコマンドを使った試験を含みます。実機での結果とは分けて記録します。

## 実機確認

RTX1210 Rev.14.01.42（整数版Lua 5.1.5・Lua機能1.08）で、掲載するコマンドと同じ固定commitのHTTPS URLを使って検証しました。自動起動設定を変更しない`no`を指定し、v0.9.9への更新後にv0.1.4へ戻しました。

| 確認項目 | 状態 |
|---|---|
| v0.1.4用コマンドのHTTPS取得・照合・起動 | 成功。固定commitのHTTPS URLから取得し、本体143,094バイトのSHA256照合・起動まで完了。 |
| v0.9.9用コマンドのHTTPS取得・照合・起動 | 成功。固定commitのHTTPS URLから取得し、本体151,437バイトのSHA256照合・起動まで完了。 |
| v0.9.9導入後のDNS応答 | 4/4成功。A・AAAA・大きなTXTのUDPからTCPへのフォールバック・ローカル名を確認。 |
| 照合不一致時の拒否 | 成功。別の版のサイズと、同じサイズで破損した本体を拒否。変更コマンドは0回で、現行ファイルを維持。 |
| v0.1.4への復帰 | 成功。試験前の本体と全バイト一致。復帰後のA・AAAA・大型TXTのTCPフォールバック・ローカル名も4/4成功。 |
| 試験後の既存config・自動起動設定・作業ファイルの確認 | 実行中configと保存済みconfigは日時等のコメント行を除いて一致。既存の自動起動設定を維持。DNSタスクは1個で、インストーラーと作業ファイルの残存なし。ルーター本体の再起動なし。 |

従来の最新安定版方式で行ったRTX1210の確認結果は[過去の検証記録](installer-2026-09-25.md)に残しています。取得方式を変更した今回の実機結果としては扱いません。

自動起動を新規登録して保存する`yes`の実機試験は、共通の更新処理に対する[従来方式の検証](installer-2026-09-25.md)で実施しています。今回の版固定変更では`yes`の実機再試験は行わず、ホスト側の回帰試験で確認しました。

### 完了メッセージの改善

同日、完了時に`Installation complete`と`Press ENTER to display the router command prompt.`を表示するようにしました。インストール処理は変更していません。再ビルドした両版について、既存のインストーラーLua試験614 assertionsとPython関連試験16件が成功しています。この表示変更後の実機再試験は行っていません。

### 9段階の進捗表示

各メッセージに`DNSINSTALL (1/9)`の形式で処理段階を表示するようにしました。SHA256の途中経過、`no`で自動起動設定を維持する場合、失敗・復旧時にも段階番号を表示します。インストールと復旧の処理順序は変更していません。既存のLua試験614 assertions、Python関連試験16件は成功しています。この表示変更後の実機再試験は行っていません。

# WebGUIインストール・アンインストール試験（2026-09-25）

ソースコード・テスト・ドキュメントはすべてOpenAI Codexで生成した。

RTX1210 Rev.14.01.42（Lua 5.1.5、Lua機能1.08）で、WebGUIの［管理］→［保守］→［コマンドの実行］から検証しました。DNS本体はv0.9.9です。

| 試験 | 結果 |
|---|---|
| 公開済みのオンラインインストール1行コマンド（yes） | 正常終了。最初の再導入は約46秒。 |
| WebGUIでの進捗表示 | Luaの進捗はコマンド実行ログに出ない。「成功」はコマンド受付を示す。 |
| インストールの完了確認 | `show status lua`で今回の正常終了と、本体タスクの継続稼働を確認。 |
| アンインストール | 約3秒で正常終了。本体タスク・標準の自動起動設定・本体ファイルを削除。 |
| 削除済みの状態で再実行 | 約3秒で正常終了。 |
| 対象外の設定 | 既存設定との差分は対象の自動起動スケジュールだけ。別のスケジュールを維持。 |
| 削除後の内蔵DNS | UDPのAレコード問い合わせは正常。Luaが受け付けていたTCP問い合わせは応答しない。 |
| アンインストール結果の記録 | Lua履歴の正常終了と`DNSUNINSTALL complete`のSYSLOGを確認。 |
| 再インストール（yes） | WebGUIから約35秒で正常終了。空いている番号で自動起動を登録。 |
| 再導入後のDNS | TCPのA・AAAA・簡易DNSの名前解決と、TXTのUDP→TCP切り替え、計4項目が正常。 |
| 再導入後の本体 | 151,437バイト。v0.9.9の公開SHA256と一致。 |
| WebGUIのコマンド制限 | `show log reverse`と`show log | grep ...`は「禁止」。`show status lua`と`show file list /lua`は実行可能。 |

利用者からも`show status lua | grep rtx-dns.lua`がWebGUIで「禁止」になるとの報告がありました。手順では、grepを付けずに実行履歴とファイル一覧を確認します。

インストーラーは固定commitの公開HTTPS URLから取得しました。アンインストーラーは公開前試験のため、同一の配布ファイルを試験用LANのHTTPサーバーから取得してWebGUIで実行しました。SYSLOG・本体のハッシュ・設定の照合はSSHからも行っています。WebGUIの「成功」表示だけで判定していません。

アンインストーラーの配布ファイルは6,480バイト、SHA256は`9f13a96f97e2c2d95a6d75777f966b682ae7820df1569f90478b340cfefff1c2`です。

ホスト側では、アンインストーラー1,858 assertions、既存インストーラー3,928 assertions、関連Pythonテスト21件が成功しました。保存・停止・削除の失敗、同時実行、Shift-JIS相当の状態表示、無関係な設定・ファイルの保持を含みます。DNSリレー本体と既存のバージョン別インストーラーは変更していません。
